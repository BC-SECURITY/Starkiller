// e2e/helpers/api/settings.js
// Settings.vue toggles client-side Pinia flags persisted to localStorage.
// No API mocks needed; this file is intentionally empty (kept so future
// settings endpoints have an obvious home).
export {};
